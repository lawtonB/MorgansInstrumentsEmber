{{display-cart}}
<div class="instruments">
  {{#each model.instruments as |instrument|}}
  {{instrument-tile instrument=instrument destroyInstrument='destroyInstrument' update='update' saveFeedback="saveFeedback"}}
  {{/each}}
</div>
<div class="new-instrument">
  {{new-instrument saveInstrument='saveInstrument'}}
</div>
