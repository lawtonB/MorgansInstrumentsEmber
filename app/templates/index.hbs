{{display-cart}}
<div class="instruments">
  {{#each model as |instrument|}}
  {{instrument-tile instrument=instrument destroyInstrument='destroyInstrument' update='update'}}
  {{/each}}
</div>
<div class="new-instrument">
  {{new-instrument saveInstrument='saveInstrument'}}
</div>
